WIRELESS DOWNLOAD - ADB


Connect to the Control Hub's Wi-Fi(18886-RC)

Open a terminal and connect with ADB

open android studio code terminal
adb disconnect
adb connect 192.168.43.1:5555

may need wifi dongle

to restart adb server:
adb kill-server
adb start-server